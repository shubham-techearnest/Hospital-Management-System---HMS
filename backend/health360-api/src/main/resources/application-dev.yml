# Optional dev profile alias — same as local for IDE runs without env vars
spring:
  config:
    activate:
      on-profile: dev

logging:
  level:
    com.health360: DEBUG

health360:
  impersonation:
    enabled: ${IMPERSONATION_ENABLED:true}

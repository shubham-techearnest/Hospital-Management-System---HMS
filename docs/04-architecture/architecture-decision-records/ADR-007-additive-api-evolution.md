# ADR-007: Additive API Evolution

| Status | PROPOSED |

## Decision
Prefer new endpoints and optional fields. Breaking changes require change request + version bump approval.

## Example
`POST /api/v1/hospital/patients/register` (new) vs altering existing patient self-service APIs.

# P1-F3-11 — Testing Strategy

| Test | Coverage |
|------|----------|
| `ClinicalTimelineIntegrationTest` | Doctor creates encounter + vitals → timeline contains ENCOUNTER_REGISTERED + CLINICAL_VITALS |
| Auth | Patient cannot call staff path without `clinical:timeline:read` |

Manual: P1-F3-14.

# P2-F1-05 — UX Requirements

Reception **Check-in** tab:

- Appointment ID (or search — MVP keeps ID field)
- Optional desk
- Primary button: **Arrive / Check in**
- Success toast shows queue token + UHID/patient if available
- Error for already checked-in appointment

Hospital OPD page: same behavior if it uses check-in.
